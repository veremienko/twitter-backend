# Overrides for the test run. Loaded after ../../.env, so these win
# (with several --env-file flags the last one takes precedence).
# A separate database and bucket keep tests away from dev data.
DATABASE_URL=postgres://twitter:twitter@localhost:5432/twitter_test
S3_BUCKET=avatars-test
